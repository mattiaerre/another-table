extends layout

block content
  script.
    const __AUTOCOMPLETE__ = {};
    __AUTOCOMPLETE__.cities = !{JSON.stringify(cities).replace(/<\//g, "<\\/")};
    __AUTOCOMPLETE__.delay = 500;
  .autocomplete
    input(type="text" id="autocomplete-input")
  script(src="javascripts/autocomplete.js")
