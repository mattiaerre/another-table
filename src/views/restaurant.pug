extends layout

block content
  .restaurant-profile-header
    .img-container
      img(src= image_url)
    .description
      h1= name
      div= price
    .contacts
      .area= area
      div= address
      div= `${city}, ${state} ${postal_code}`
      div= phone
